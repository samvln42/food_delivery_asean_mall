# 📍 คู่มือการตั้งค่าพิกัดร้านอาหาร

## ✅ สิ่งที่ทำเสร็จแล้ว

1. ✅ เพิ่มฟิลด์ `latitude` และ `longitude` ในฟอร์มจัดการร้าน (AdminRestaurants.jsx)
2. ✅ เพิ่มปุ่ม "🔍 ค้นหาพิกัดจากที่อยู่" เพื่อ geocode อัตโนมัติ
3. ✅ อัปเดต API ให้รับและบันทึกพิกัดร้าน

---

## 🚀 วิธีใช้งาน

### สำหรับ Admin

1. ไปที่หน้า **Admin > Restaurants**
2. คลิก **Edit** ที่ร้านที่ต้องการ
3. กรอกข้อมูล:
   - **ที่อยู่** (Address)
   - คลิกปุ่ม **"🔍 ค้นหาพิกัดจากที่อยู่"** เพื่อค้นหาพิกัดอัตโนมัติ
   - หรือกรอก **Latitude** และ **Longitude** ด้วยตนเอง

### วิธีกรอกพิกัดด้วยตนเอง

1. เปิด Google Maps
2. ค้นหาที่อยู่ของร้าน
3. คลิกขวาที่ตำแหน่งร้าน
4. เลือกพิกัด (Coordinates)
5. คัดลอก Latitude และ Longitude มาใส่ในฟอร์ม

**ตัวอย่าง:**
- Latitude: `17.9668552`
- Longitude: `102.6427002`

---

## 📝 หมายเหตุสำคัญ

### พิกัดจำเป็นสำหรับการคำนวณค่าจัดส่ง

- ✅ ถ้ามีพิกัด → ระบบจะคำนวณค่าจัดส่งตามระยะทาง (ระยะทางจริง)
- ❌ ถ้าไม่มีพิกัด → ระบบจะใช้วิธีเดิม (ตามจำนวนร้าน)

### การตรวจสอบ

หลังจากบันทึกพิกัดแล้ว:
1. ตรวจสอบว่าพิกัดถูกบันทึกในฐานข้อมูล
2. ทดสอบคำนวณค่าจัดส่งในหน้า Cart
3. ตรวจสอบว่าค่าจัดส่งคำนวณตามระยะทางจริง

---

## 🔍 วิธีตรวจสอบว่าพิกัดถูกบันทึกแล้ว

1. ไปที่หน้า Admin > Restaurants
2. คลิก **View** ที่ร้าน
3. ดูที่ฟิลด์ **Latitude** และ **Longitude**
4. ถ้ามีค่าแสดงว่าบันทึกสำเร็จแล้ว

---

## 💡 Tips

- **ใช้ปุ่ม "ค้นหาพิกัดจากที่อยู่"** เพื่อความสะดวก
- ถ้าค้นหาไม่เจอ ให้กรอกพิกัดด้วยตนเอง
- พิกัดควรมีทศนิยมอย่างน้อย 6-7 ตำแหน่งเพื่อความแม่นยำ

---

## ✅ เสร็จสมบูรณ์!

ตอนนี้สามารถตั้งค่าพิกัดร้านได้แล้ว:
- ✅ กรอกพิกัดในฟอร์ม
- ✅ ค้นหาพิกัดอัตโนมัติจากที่อยู่
- ✅ บันทึกพิกัดในฐานข้อมูล
- ✅ ใช้พิกัดคำนวณค่าจัดส่งตามระยะทาง

