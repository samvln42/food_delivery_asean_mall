# API Configuration - ตั้งค่า URL ของ backend API
# สำหรับ production server
VITE_API_URL=https://tacashop.com/api/

# สำหรับ local development (uncomment เมื่อพัฒนาแบบ local)
# VITE_API_URL=http://127.0.0.1:8000/api/

# Environment mode (สำหรับ performance optimization)
# VITE_NODE_ENV=production

# หมายเหตุสำคัญ: 
# - WebSocket จะถูกตั้งค่าอัตโนมัติตาม VITE_API_URL
# - ไม่ใช้ hardcoded fallback URLs ในโค้ด
# - ต้องมี .env ไฟล์ที่มี VITE_API_URL เสมอ